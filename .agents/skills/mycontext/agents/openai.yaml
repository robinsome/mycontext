interface:
  display_name: "Mycontext"
  short_description: "Repo-specific patterns and workflows for mycontext"
  default_prompt: "Use the mycontext repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true